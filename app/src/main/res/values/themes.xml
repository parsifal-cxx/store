<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Store" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.Store.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_accent</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_bag</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_block</item>
        <item name="postSplashScreenTheme">@style/Theme.Store</item>

    </style>

</resources>